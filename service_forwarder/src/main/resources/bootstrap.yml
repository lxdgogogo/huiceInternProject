spring:
  application:
    name: service_forwarder
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848

      config:
        server-addr: http://localhost:8848
        encode: UTF-8
        file-extension: yaml