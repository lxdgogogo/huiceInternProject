spring:
  application:
    name: fdk-service
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848
        namespace: hc-summer-2

      config:
        server-addr: http://localhost:8848
        encode: UTF-8
        file-extension: yaml #指定yaml格式的配置
        namespace: hc-summer-2
