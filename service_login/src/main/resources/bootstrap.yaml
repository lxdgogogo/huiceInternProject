server:
  port: 8087

spring:
  application:
    name: summer-user
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848
        namespace: hc-summer-2

      config:
        server-addr: http://localhost:8848
        encode: UTF-8
        namespace: hc-summer-2
